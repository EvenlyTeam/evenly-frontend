import { Base } from './Base';
import { Body } from './Body';
import { Footer } from './Footer';
import { Header } from './Header';

export const SharedSettlementStyleWrapper = Object.assign(Base, {
  Header,
  Body,
  Footer,
});
